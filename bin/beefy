#!/usr/bin/env node
require('../cli')(
    process.argv.slice(2)
  , process.cwd()
  , process.stdout
  , process.stderr
  , onclose
) 

function onclose(err) {
  // noop
  if(err) {
    process.exit(1)
  }
}
